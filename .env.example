# Copy to .env or point DOTENV_PATH to a shared secrets file (e.g. on PA).

MA_LENDER_ID=
MA_API_KEY=

# Optional: absolute path to a single env file (overrides default .env in project root)
# DOTENV_PATH=/home/sergiovegadev93/.secrets/lmc.env

EXCEL_PATCHER_PATH=
EXCEL_TRACKER_PATH=

# Optional Anthropic key if you add AI summaries later
# ANTHROPIC_API_KEY=

# Optional: used with `py -3 orchestrator.py --verify --verify-api`
# VERIFY_LOAN_ID=12345
